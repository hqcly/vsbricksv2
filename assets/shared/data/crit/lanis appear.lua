function onBeatHit()
    if curBeat == 236 then
        addAnimationByPrefix('lanis', 'idle', 'idle', 24, false)
    end
    if curBeat == 252 then
        playAnim('lanis', 'idle', 'idle', false, 0)
    end
    if curBeat == 332 then
        playAnim('lanis', 'idle', 'idle', false, 0)
    end
    if curBeat == 424 then
        playAnim('lanis', 'idle', 'idle', false, 0)
    end
    if curBeat == 424 then
        playAnim('lanis', 'idle', 'idle', false, 0)
    end
    if curBeat == 520 then
        playAnim('lanis', 'idle', 'idle', false, 0)
    end
end